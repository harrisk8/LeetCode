var deleteAndEarn = function(nums) {

    let points = {};

    for (number of nums) {
        console.log(number);
        (!points.hasOwnProperty(number)) 
    }
    
};

let nums = [3, 4, 2];

deleteAndEarn(nums);